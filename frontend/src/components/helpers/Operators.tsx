type ForProps = {
    startsIn: number,
    endsIn: boolean,
    next: Function,
}

type ForEachProps = {
    array: Array<Object|number|string>,
    trackBy: number|string,
}

export function For({startsIn, endsIn, next}: ForProps) {
    
}

export function ForEach({array, trackBy}: ForEachProps) {
    
}